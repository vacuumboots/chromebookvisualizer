{
  "_comment": "Example config for the Chromebook Fleet Ratio Visualizer. Copy this file to data/config.json and adjust the values for your organization. data/ is git-ignored, so your real config never gets committed. All fields are optional; omit any you don't need.",

  "ouPrefix": "/your/ou/prefix/",
  "groupingDepth": 1,

  "excludedPathPatterns": [
    "/staff",
    "exam"
  ],

  "ratioThresholds": {
    "good": 1.2,
    "acceptable": 1.5
  },

  "dashboardTitle": "Student-to-Chromebook Ratio Visualizer"
}
